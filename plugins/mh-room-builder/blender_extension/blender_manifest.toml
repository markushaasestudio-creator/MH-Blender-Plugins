schema_version = "1.0.0"
id = "mh_room_builder"
version = "0.1.3"
name = "MH Room Builder"
tagline = "Dimensioned floorplans, human review, and local semantic AI"
maintainer = "MH Studio"
type = "add-on"
blender_version_min = "5.2.0"
license = ["SPDX:GPL-3.0-or-later"]

[permissions]
files = "Read floorplans, analysis files, reference images, and local training examples"
network = "Connect only to the user-configured Ollama endpoint; non-local endpoints require explicit opt-in"
